---
- name: reload systemd
  systemd:
    daemon_reload: yes

- name: restart bird
  systemd:
    name: bird
    state: restarted
    daemon_reload: yes

- name: restart vault-healthcheck timer
  systemd:
    name: vault-healthcheck.timer
    state: restarted
    enabled: yes
    daemon_reload: yes